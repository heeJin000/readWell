import React, {Component} from 'react';
import ApiCall from './ApiCall';

const Title = () => {
      return(
        <header>
          <h1>GoodReader</h1>     
        </header>
      );
  }

export default Title;